CREATE TABLE `student` (
  id             int,
  code           VARCHAR(64),
  age            int,
  overall_rating long,
  created_at     datetime(3),
  readOnly       VARCHAR(100),
  PRIMARY KEY (`id`)
);

